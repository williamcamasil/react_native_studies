import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  button: {
    backgroundColor: '#F1BB00',
    width: 300,
    borderRadius: 5,
    padding: 5,
    alignItems: 'center',
    fontWeight: 'bold',
  },
});
